#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int x;
    cin >> x;
    if (x >= 200)
    {
        cout << x - 50 << endl;
    }
    else
    {
        cout << x << endl;
    }
    return 0;
}
